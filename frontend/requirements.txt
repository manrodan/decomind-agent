# Decomind Agent UI — backend deps.
# Stack: FastAPI HTTP + ADK runner directo + MCP HTTP clients.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.27.0
google-auth>=2.30.0

# ADK orchestrator + Vertex AI / Gemini
google-adk>=0.5.0
google-genai>=1.0.0
google-cloud-aiplatform>=1.70.0

# MCP client (httpx-based streamable-http transport)
mcp>=1.0.0

# Dotenv para que agent.main pueda cargar agent/.env en local
python-dotenv>=1.0.0
